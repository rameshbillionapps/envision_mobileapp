import {api, setToken} from './api';
import globalColors from './commonColor';
import globalImages  from './globalimage';
import {deleteKey,clearAllKey,updateKey,saveKey,getKey}  from './DeviceStorage';

export {
   api,
   setToken,
   globalImages,
   globalColors,
   deleteKey,
   clearAllKey,
   updateKey,
   saveKey,
   getKey
}
